# Innovation Hub — Student-Project Matching CLI

Fully offline matching tool for placing students into company projects.
No data leaves your machine.

## Quick start

```bash
pip install -r requirements.txt
python main.py --help
```

## Install man pages

```bash
python main.py --generate-man
bash install.sh
```
