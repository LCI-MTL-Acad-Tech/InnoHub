"""
dashboard_web.py — local HTTP server for the visual dashboard.
Binds to 127.0.0.1 only. Serves static HTML + JSON API endpoints.
No external dependencies beyond Python stdlib.
"""
# Full implementation will cover:
#   - http.server.BaseHTTPRequestHandler subclass
#   - GET /api/status, /api/programs, /api/students, /api/companies,
#         /api/projects, /api/assignments, /api/document/<type>/<id>
#   - embedded single-file HTML dashboard (served at GET /)
#   - --port and --semester options
pass
